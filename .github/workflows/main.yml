on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: hydephp/action@add-root-directory-path-option
        with:
          directory: 'custom-dir'
